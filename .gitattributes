# Preserve the byte-stable, self-hashed Phase 1 import package on every platform.
pro_a_r1_node_import_package_B2C_FINAL_QUALIFIED_20260819/** text eol=lf

# Preserve byte-stable, source-text-free Phase 3F public audit artifacts.
workspace/phase3f_stage1_public_audit/** text eol=lf

# Authorized migration is a byte identity, not a platform-normalized text file.
src/pro_a/migrations/foundation_0_2_3_relation_native.sql -text

# Keep the accepted v0.4.0 canonical runtime byte identities on every platform.
src/pro_a/phase4_orchestration.py -text
src/pro_a/phase4_retry.py -text
src/pro_a/phase4_replay.py -text
src/pro_a/phase4_source_audit.py -text
src/pro_a/phase4_requalification.py -text
src/pro_a/corpus_pilot.py -text
src/pro_a/operational_ingestion.py -text
src/pro_a/production_authorization.py -text
src/pro_a/table_claim_safety.py -text

# Public historical prompt fixture binds the unchanged public Git blob.
src/pro_a/prompts.py text eol=lf

# Keep frozen Phase 4.3 Stage 2 human-review presentation hashes on Windows.
docs/HUMAN_QUALIFICATION_PACKET_SELF_CONTAINED.md text eol=lf whitespace=-blank-at-eof
docs/phase43_stage2_human_decision_template.json text eol=lf
docs/phase43_stage2_human_authorization.json text eol=lf

# Domain pack inventories bind exact file bytes on every checkout platform.
# Treat preserved CRLF as line endings while retaining normal whitespace checks.
domains/** -text whitespace=blank-at-eol,blank-at-eof,space-before-tab,cr-at-eol

# Research navigation specs expose their exact UTF-8 file SHA256 through the API.
research_navigation/*.json text eol=lf
