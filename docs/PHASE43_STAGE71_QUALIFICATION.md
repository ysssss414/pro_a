# Phase 4.3 Stage 7.1 qualification

Processing scope is not research Domain classification. This stage qualifies `SHARED_CORE_PENDING` intake on disposable schema 11 Workbench fixtures. The real Workbench and Production databases are read-only throughout. No real Domain Pack registration, ZSXQ call, live provider call, Production Apply, or Human Review decision is part of qualification.

## Contract and control evidence

- `run-domain-context-v1` remains the explicit Domain-assigned, `OFFLINE_REPLAY_ONLY` contract. A non-fake provider remains blocked with `DOMAIN_ACTIVATION_REQUIRED`.
- `run-processing-context-v2` freezes the Shared Core identity, source, runtime, prompt, configuration, model profile, and `LIVE_SHARED_CORE_BOUNDED` policy. It uses no Domain assignment row or Pack. A typed non-network provider double exercises the durable provider seam; existing CloudJobs identity and budget checks remain in force.
- Generic and Community Sources with zero registered Packs and zero Domain assignments reach `HUMAN_REVIEW_REQUIRED` using the deterministic fake provider. Community preview creates no Source; import keeps low-trust provenance and ordered events. An explicit registered Domain still uses v1.
- A later operator Domain assignment leaves the pending run, context, packet, and Stage 1 Domain filters unchanged. A new assigned run requires an explicit reprocess reason. Pending packet rows count toward Stage 1 capacity.

## Product acceptance

Disposable browser acceptance covered a canonical synthetic Company Community import at 1920×1080 and a generic PDF Source at 1366×768. The zero-Pack Community page displayed Shared Core as the default, enabled import, and showed pending scope in Source detail and Review. Both synthetic runs reached `HUMAN_REVIEW_REQUIRED`. The disposable Company Materials timeline shows the Community Source as private, noncanonical, low-trust, and Domain pending. Page scroll widths equaled viewport widths at both sizes.

## Test policy

The focused Stage 7.1 tests and Stage 0/7 regressions passed in the 263-test relevant backend run. The full frontend suite passed 159/159 tests in 29 files. TypeScript build, Python compile, and diff checks passed. Full repository pytest completed with 2391 passed, 6 skipped, 4 failed, and 29 errors. Its 33 fail/error node IDs and statuses match the Stage 7 audited historical/environment exception list exactly; the 14 additional passes are the Stage 7.1 tests. No new exception remains unresolved. The diagnostic is intentionally non-green because the historical private Foundation and frozen Production-binding prerequisites are absent in this workspace.

## Real state and delivery

Read-only checks confirm schema 11, zero registered Domain Packs, zero private Sources, zero processing runs, WIP `OPEN`, zero operational pending rows, and 274 historical lifecycle closures. The exact before and after Production and Workbench hashes, test results, and gates are recorded in `phase43_stage71_qualification_receipt.json`. Draft PR metadata is verified in the joint pre-merge audit receipt. The next separately controlled pilot may use a real Community Source under Shared Core Pending and stop at Human Review.

Required checks:

1. Stage 0 v1 context validation, fake provider path, and nonfake `DOMAIN_ACTIVATION_REQUIRED` remain intact.
2. A generic clean PDF and a Community ZIP can reach `HUMAN_REVIEW_REQUIRED` with zero registered Domain Packs and zero Source Domain assignments.
3. The v2 context rejects changed scope, shared-core identity, prompt, runtime, configuration, model configuration, and Source identity.
4. A later assignment leaves the old context, packet, Review projection, and Domain filter membership unchanged. A new assigned run requires an explicit reprocess reason.
5. Relevant Python tests, the frontend suite, TypeScript, build, compileall, browser flows at 1920×1080 and 1366×768, and `git diff --check` pass before delivery.

The machine-readable receipt in `phase43_stage71_qualification_receipt.json` records observed outcomes and real-state hashes. The full Python suite is compared with the audited Stage 7 historical/environment exception population; new Stage 7.1 regressions must be zero.
